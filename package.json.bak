{
  "name": "git-daily-report-dev",
  "version": "1.0.0",
  "description": "Development dependencies for git-daily-report",
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --target=node18 --outfile=dist/cli.js",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "commander": "^12.1.0",
    "dotenv": "^16.4.5",
    "esbuild": "^0.27.0",
    "typescript": "^5.7.2"
  }
}
